{
  "mcpServers": {
    "filesystem-http-home": {
      "command": "node",
      "args": ["dist/index.js", "--http", "--port", "8080", "$HOME/allowed/path"]
    },
    "filesystem-http-env": {
      "command": "node",
      "args": ["dist/index.js", "--http", "--port", "8080", "${ALLOWED_PATH}"]
    }
  }
}
